# Copy to .env and adjust as needed
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=3001
# Optional: absolute path for uploaded files (defaults to server/uploads)
# UPLOAD_DIR="C:/path/to/uploads"
# Optional: override the permanent admin wallet (defaults to the hardcoded one in src/config.js)
# ADMIN_WALLET="0x..."
