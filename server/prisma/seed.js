// No seed data — all users, requests, votes, and ledger entries
// are created by real wallet connections and real actions.
// The first wallet to connect automatically becomes admin.
console.log("No seed needed — database starts clean.");
